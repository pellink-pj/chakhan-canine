Selectable chip for filters and quick options (breed size, group, lifestyle tags).

```jsx
<Chip selected={active} onClick={toggle}>활동량 많음</Chip>
```

Toggles to coral-tinted + coral border when `selected`. Use in a flex/gap row. An optional `icon` (Lucide node) is supported but unused by default.
